# Global env file for the monorepo (root). Copy to .env and fill in values.
# Shared by apps/web (via vite envDir) and apps/api (via ConfigModule).

# --- api ---
PORT=3000

# --- web ---
VITE_API_URL=http://localhost:3000
